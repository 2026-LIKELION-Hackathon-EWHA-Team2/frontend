// 로그인이 필요한 페이지에 접근할 때 사용할 커스텀 훅
import { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import useUserStore from '../store/useUserStore';

const useAuth = () => {

};

export default useAuth;